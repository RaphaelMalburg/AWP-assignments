@{
    ViewData["Title"] = "Home Page";
}

@if (Model != null)
{
    @Html.Raw(Model.Title)
    @Html.Raw(Model.Text)
}
else
{
    <div class="text-center">
        <h1 class="display-4">Welcome</h1>
        <p>Content not found. Please configure content in the admin panel.</p>
    </div>
}